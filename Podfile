# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'pset6' do
    pod 'Firebase/Auth'
    pod 'Firebase/Core'
    pod 'Firebase/Database'
    use_frameworks!
    
    target 'pset6Tests' do
        inherit! :search_paths
        pod 'Firebase'
    end
    
    target 'pset6UITests' do
        inherit! :search_paths
        pod 'Firebase'
    end
end
